import React from 'react';
import {
    View,
    Text,
    TouchableOpacity,
    Image
} from 'react-native';
import { Container, Content, Text, Accordion, Card, CardItem, Left, Radio, Right, Thumbnail, List, Icon, Body, ListItem } from "native-base";

import styles from './styles';

import logo from '../../../img/logo_so.png'

//COMPONENTES

const RadioMenu = ({ onPress, children, color, title, desc, preco }) => {

    return (
        <View>
            
        </View>
    )
};

export default RadioMenu;

/*
() => this.props.navigation.navigate('DrawerOpen')
 */